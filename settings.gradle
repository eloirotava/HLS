pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Pode remover o maven da appodeal, não precisamos mais dele
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "HLSStreamer"
include ':app'