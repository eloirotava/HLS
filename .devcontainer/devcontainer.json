{
  "name": "Android HLS Env",
  // Usamos uma imagem da comunidade que já tem Java + Android SDK pronto
  "image": "mobiledevops/android-sdk-image:34.0.0-jdk17",
  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "mathiasfrohlich.Kotlin",
        "androiddev.android-manifest-snippets"
      ]
    }
  },
  "remoteUser": "root",
  "postCreateCommand": "apt-get update && apt-get install -y gradle && echo 'Ambiente Recuperado!'"
}